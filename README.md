# DisasterMap

This repository is part of the final project for the UM_CISC7204 course. This project uses EM-DAT data to build an interactive map platform.

## Run

```sh
npm install
npm run dev
```
After running the above command, access [http://localhost:5173](http://localhost:3000) in your browser.